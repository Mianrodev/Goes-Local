# v15.88-shared (25 Sep 2026)

Cost plan Phase C step 8, approved by Eric ("go with B").

- A listing's sub-category is now only the **first** business type from GoHighLevel's combined list ("Florist, Flower Delivery, Flower Designer" → "Florist"). Miami: ~2,783 sub-category pages → ~1,000. Orlando has no combined types (unchanged, 866). The homepage "All N categories" number drops accordingly.
- Main categories are unchanged: they're still worked out from the full value, exactly as before. (Tried also filing combined types by their first type via `SUBMAP` — the simulation moved 628 listings, many into wrong categories because `SUBMAP` has bad entries, so that was not shipped.)
- Old combined sub-category URLs 301 to the new short one: for `/{cat}/{x}` that isn't a listing or a known sub-category, redirect to the longest known sub-category slug of that category that `x` starts with (e.g. `/shopping-and-boutiques/florist-flower-delivery-flower-designer` → `/shopping-and-boutiques/florist`), keeping the query string.
- Takes effect as the listing sync rewrites the ~3,600 affected Miami rows (one pass, ~30 min).
