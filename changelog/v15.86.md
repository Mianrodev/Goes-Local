# v15.86-shared (25 Sep 2026)

Cost plan Phase C (crawl space), steps 6–7. Step 8 (shortening the ~1,900 combined sub-category URLs) is on hold: Eric asked to keep the big "All N categories" number, which that change would shrink — needs his decision.

- robots.txt, every user-agent group: also `Disallow: /*?*hood=`, `/*?*sub=`, `/search` (v15.81 already blocked `rating`, `claim`, `sort`); `Crawl-delay: 5` for `*`. Base category, sub-category (`/{cat}/{sub}`), neighbourhood (`/neighbourhood/{slug}`), business pages and `?page=` stay crawlable; sitemap line kept.
- Filtered/sorted views (`hood`, `rating`, `claim`, `sort`, `sub` params) and `/search` send `X-Robots-Tag: noindex, follow`. Their canonical already pointed to the unfiltered page.
- Filter links in the category sidebar/drawer get `rel="nofollow"`.
