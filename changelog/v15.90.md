# v15.90-shared (25 Sep 2026)

QA fixes approved by Eric + AEO.

- **Garbled names repaired.** `FIXMOJI()` reverses UTF-8 text that was mis-read as Windows-1252 ("Floridaâ€™s" → "Florida’s", "GonzÃ¡lez" → "González", "MÃ˜" → "MØ"), including the lost-byte case ("Ãlvarez" → "Álvarez"). Applied in `norm()` to name, address, sub-category and description, so the sync repairs ~165 listings per city on its next pass (renamed listings get new slugs; old URLs 301 via `slug_redirects`). Leaves text alone if a repair would drop a character mid-string (mixed good/garbled text). Tested on all 24 garbled names sampled from both cities: all fixed, controls untouched.
- **Forms no longer say "thanks" after a failed save:**
  - List your business: error page only if the database save failed *and* no CRM contact was created (otherwise the team still has it).
  - Delete-listing and cancel-plan requests (owner dashboard): error page if the request wasn't stored.
  - Reviews, customer photos, blog comments: error page on a failed save; "please slow down" page instead of a silent drop when the hourly limit is hit.
  - Enquiry form: "sent to the business" only when the owner was actually emailed; "our team will pass it on" when it was saved to the CRM only (most unclaimed listings); error page when nothing was saved.
  - Admin → Businesses: shows an error if the custom-code box failed to save.
- **Payment webhook** (`/webhooks/ghlpay`): returns 502 when tagging the listing in GoHighLevel fails, so GHL retries instead of the customer silently staying un-upgraded. `ghlAddTag`/`ghlRemoveTag` now return whether they worked (existing callers unaffected).
- **llms.txt**: removed the `/search` link (search is disallowed in robots.txt since v15.86).
