# v15.83-shared (25 Sep 2026)

Follow-up to v15.82 (Miami went from 132 to 25 main categories after the first sync pass; 336 listing URLs recorded in `slug_redirects`).

- Removed-category redirect picks the main category where that business type has the most listings (was: the first match, which sent `/window-tinting-service` to Cleaning because of 1 misfiled listing instead of Automotive with 37).
- Old stray main-category pages whose type only exists inside combined types (e.g. `/toyota-dealer`) now 301 to the main category most of their listings moved to, using `slug_redirects` (e.g. `/toyota-dealer` → `/auto-sales-and-rental`).
